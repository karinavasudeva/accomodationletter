{
  "name": "accommodation-generator",
  "version": "1.0.0",
  "description": "Accommodation letter generator using Claude API",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "axios": "^0.21.1"
  },
  "devDependencies": {
    "dotenv": "^10.0.0",
    "vercel": "^24.0.0"
  }
}
